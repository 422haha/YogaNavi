package com.yoga.backend.mypage.recorded;

/*
import com.yoga.backend.mypage.recorded.dto.LectureDto;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.util.List;

@RestController
@RequestMapping("/mypage/recorded-lecture")
public class RecordedController {

    @Autowired
    private RecordedService recordedService;

    */
/**
     * 사용자가 업로드한 강의 목록을 조회
     *
     * @return 사용자가 업로드한 강의 목록과 관련된 정보가 포함된 ResponseEntity 객체
     *//*

    @GetMapping("/{email}")
    public ResponseEntity<List<LectureDto>> getMyLectures(@PathVariable String email) {
        return null;
    }

    */
/**
     * 새로운 강의를 업로드
     *
     * @param lectureDto 강의 dto
     * @return 업로드된 강의의 정보가 포함된 ResponseEntity 객체
     *//*

    @PostMapping
    public ResponseEntity<LectureDto> uploadLecture(@RequestParam("title") LectureDto lectureDto) {
        return null;
    }

    */
/**
     * 업로드된 강의를 수정
     *
     * @param lectureDto 강의 dto
     * @return 수정된 강의의 정보가 포함된 ResponseEntity 객체
     *//*

    @PutMapping("/{id}")
    public ResponseEntity<LectureDto> updateLecture(LectureDto lectureDto) {
        return null;
    }

    */
/**
     * 업로드된 강의를 삭제
     *
     * @param id 삭제할 강의의 ID
     * @return 삭제 성공 여부를 나타내는 ResponseEntity 객체
     *//*

    @DeleteMapping("/{id}")
    public ResponseEntity<Void> deleteLecture(@PathVariable Long id) {

        return null;
    }
}*/
